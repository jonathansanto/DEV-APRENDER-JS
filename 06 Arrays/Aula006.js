// Conjunto de dados que pode ser acessado por um indice
// indíce - posição exata onde se encontra uma informação dentro do conjunto de informações


let familia = [25,45,'jonathan',10];
console.log(familia.length);
console.log(familia[2]);

let nomeDoColega = [];
